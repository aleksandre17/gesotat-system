# Host boundary doctrine

`agent-framework/` is upstream-only. `.agents/kit` is a versioned attachment point and never the source of kit truth. Host-specific code, data, operations and evidence belong under the host boundary.
