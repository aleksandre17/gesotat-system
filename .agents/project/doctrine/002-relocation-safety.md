# Relocation safety doctrine

No source directory is deleted or moved until a manifest, dependency graph, reproducible build, migration check and rollback proof pass. Existing GEOSOTAT paths remain authoritative while the platform boundary is scaffold-only.
