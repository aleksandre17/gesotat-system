# Mobile composition boundary

The mobile service is declared by the project-level Compose entrypoints. Keep
mobile-only overrides and profile fragments here; never duplicate the canonical
service definition or place API configuration in this directory.
