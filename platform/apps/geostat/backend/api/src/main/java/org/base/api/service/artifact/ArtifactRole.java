package org.base.api.service.artifact;

/** Semantic role of an attachment (DCAT distribution vs. supporting material); mirrors {@code ck_artifact_relation_role}. */
public enum ArtifactRole { PRIMARY, SUPPORTING, DERIVED, PREVIEW }
