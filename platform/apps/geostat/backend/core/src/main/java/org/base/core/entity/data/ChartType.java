package org.base.core.entity.data;

/** Chart types supported by the initial declarative grammar. */
public enum ChartType {
    TABLE,
    LINE,
    BAR,
    STACKED_BAR,
    AREA,
    PIE,
    DONUT
}
