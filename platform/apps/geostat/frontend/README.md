# Frontend target boundary

Canonical target for the existing `web` module. The source remains authoritative at `web/` and is mounted below this boundary by a reversible junction so the current build and runtime contract remain reproducible.
